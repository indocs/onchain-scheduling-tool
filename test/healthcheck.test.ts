import { expect } from "chai";

describe("Healthcheck", () => {
  it("sanity check passes", async () => {
    // Minimal sanity test to ensure test suite runs reliably
    expect(true).to.equal(true);
  });
});
